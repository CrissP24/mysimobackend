# Backend Environment Example (Neon + Prisma)

# Use POOLER URL for runtime (Prisma Client)
# Add pgbouncer=true and connection_limit=1, always require SSL
DATABASE_URL="postgresql://USER:PASS@ep-xxxxx-pooler.us-east-1.aws.neon.tech/mysimo?sslmode=require&pgbouncer=true&connection_limit=1"

# Use DIRECT URL for migrations (Prisma migrate)
DIRECT_URL="postgresql://USER:PASS@ep-xxxxx.us-east-1.aws.neon.tech/mysimo?sslmode=require"

# JWT secret for signing tokens
JWT_SECRET="super_secret_change_me"

# Server port
PORT=4000

# CORS allowed origins (comma separated)
CORS_ORIGINS=http://localhost:5173

